# Only used by the Emergent preview environment where /api/* is
# routed to this FastAPI process on port 8001. In production this backend
# is not deployed; frontend/ Next.js serves /api/* natively.
MONGO_URL=mongodb://localhost:27017
DB_NAME=jacknjillkids
